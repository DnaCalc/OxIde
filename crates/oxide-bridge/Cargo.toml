[package]
name = "oxide-bridge"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
oxide-core = { path = "../oxide-core" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
