[package]
name = "oxide-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
oxide-domain = { path = "../oxide-domain" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
