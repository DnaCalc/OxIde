[package]
name = "ox-ide"
version = "0.1.0"
edition = "2024"
description = "Console micro-IDE companion for OxVba"
publish = false

[dependencies]
ftui = { version = "0.2.1", default-features = false, features = ["crossterm"] }
