# DNA OxIde Tauri Capabilities Placeholder

W341 keeps Tauri permissions conservative. The `default.json` file exists so later Tauri work has a capabilities location, but no runtime, debug, Immediate, native COM, filesystem broadening, or sidecar permissions are claimed here.
